# Use single quotes around bcrypt hashes to preserve dollar signs.
READER_HOST=localhost
BIND_ADDRESS=127.0.0.1
READER_AUTH_ENABLED=false
# Required only when READER_AUTH_ENABLED=true.
READER_PASSWORD_HASH=''
